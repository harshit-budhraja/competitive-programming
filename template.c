/* Author:- arachnis */
// Header Files
#include<stdio.h>
#include<stdlib.h>
#include<math.h>
#include<string.h>

// Variable declarations and preprocessor directives
int i=0,x=0;
char *str;
#define ll long long int
#define _loop(i,s,n,x) for(i=s;i<n;i+=x)
#define print_num(i) printf("%d",i)
#define print_str(str) printf("%s",str);
#define testcases(i) while(i--)
#define newline() printf("\n")

// Driver function
int main()
{
	
	return 0;
}
