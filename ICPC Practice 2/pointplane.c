#include<stdio.h>

void main()
{
	int T;
	long int n;
	scanf("%d",&T);
	while(T--)
	{
		scanf("%ld",&n);
		long long int x[n],y[n];
		int i;
		for(i=0;i<n;i++)
			scanf("%lld %lld",&x[i],&y[i]);
		for(i=0;i<
		if(n==2)
		{
			print("yes\n");
		}
		else
		{
			if(y[1]-y[]
				
				
